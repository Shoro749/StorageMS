<Window x:Class="UI.Windows.OrderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UI.Windows"
        mc:Ignorable="d"
        Title="OrderWindow" Height="500" Width="450"
        Background="#121212" WindowStartupLocation="CenterOwner" WindowStyle="None" AllowsTransparency="True">
    <Grid>
        <Grid.Resources>
            <Style TargetType="DataGridColumnHeader">
                <Setter Property="Background" Value="#1E1E1E"/>
                <Setter Property="Foreground" Value="#D2B48C"/>
                <Setter Property="Padding" Value="10,5"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="BorderThickness" Value="0,0,0,1"/>
                <Setter Property="BorderBrush" Value="#333333"/>
            </Style>

            <Style TargetType="ComboBox">
                <Setter Property="Background" Value="#2D2D2D"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="BorderBrush" Value="#333333"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Padding" Value="8,5"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ComboBox">
                            <Grid>
                                <Border x:Name="Border" Background="{TemplateBinding Background}" 
                    BorderBrush="{TemplateBinding BorderBrush}" 
                    BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="5"/>

                                <Grid Margin="{TemplateBinding Padding}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <ContentPresenter Content="{TemplateBinding SelectionBoxItem}" VerticalAlignment="Center"/>
                                    <Path Grid.Column="1" Data="M0,0 L4,4 L8,0" Stroke="#D2B48C" StrokeThickness="2" VerticalAlignment="Center" Margin="5,0,0,0"/>
                                </Grid>

                                <Popup x:Name="PART_Popup" AllowsTransparency="True" IsOpen="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}" Placement="Bottom">
                                    <Border Background="#1E1E1E" BorderBrush="#333333" BorderThickness="1" CornerRadius="5" MinWidth="{TemplateBinding ActualWidth}">
                                        <ItemsPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                                    </Border>
                                </Popup>
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Border" Property="BorderBrush" Value="#D2B48C"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style TargetType="ComboBoxItem">
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="Padding" Value="10,8"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ComboBoxItem">
                            <Border x:Name="Gd" Background="Transparent" Padding="{TemplateBinding Padding}">
                                <ContentPresenter />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Gd" Property="Background" Value="#333333"/>
                                    <Setter Property="Foreground" Value="#D2B48C"/>
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter TargetName="Gd" Property="Background" Value="#44D2B48C"/>
                                    <Setter Property="Foreground" Value="#D2B48C"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </Grid.Resources>

        <Border BorderBrush="#333333" BorderThickness="1" CornerRadius="15" Background="#1E1E1E">
            <Grid Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="РЕДАГУВАННЯ СКЛАДУ ЗАЯВКИ" Foreground="#D2B48C" 
                   FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>

                <StackPanel Grid.Row="1" Margin="5,0,5,15">
                    <TextBlock Text="Статус заявки" Foreground="#555555" FontSize="10" Margin="0,0,0,2"/>
                    <ComboBox x:Name="cb_Status" Height="35" VerticalContentAlignment="Center">
                        <ComboBoxItem Content="Pending"/>
                        <ComboBoxItem Content="Completed"/>
                        <ComboBoxItem Content="Canceled"/>
                    </ComboBox>
                </StackPanel>

                <Border Grid.Row="2" Background="#181818" CornerRadius="8" Padding="5">
                    <DataGrid x:Name="dg_OrderItems" Background="Transparent" Foreground="White" 
                      AutoGenerateColumns="False" CanUserAddRows="False" GridLinesVisibility="None">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Товар" Binding="{Binding Product.Name}" Width="*"/>
                            <DataGridTextColumn Header="Доступно" Binding="{Binding Product.Stock}" Width="80"/>
                            <DataGridTemplateColumn Header="К-сть" Width="80">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBox Text="{Binding Quantity, UpdateSourceTrigger=PropertyChanged}" 
                                         Background="#2D2D2D" Foreground="White" BorderThickness="0"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Width="50">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="🗑" Foreground="#E53935" Background="Transparent" 
                                        BorderThickness="0" Click="RemoveItem_Click"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>

                <Border Grid.Row="3" Background="#252525" CornerRadius="8" Margin="0,15" Padding="10">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Margin="5">
                            <TextBlock Text="Виберіть товар" Foreground="#888888" FontSize="10"/>
                            <ComboBox x:Name="cb_ProductPicker" DisplayMemberPath="Name" 
                              SelectionChanged="cb_ProductPicker_SelectionChanged"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1" Margin="5" Width="60">
                            <TextBlock Text="Залишок" Foreground="#888888" FontSize="10"/>
                            <TextBlock x:Name="txt_Available" Text="0" Foreground="#D2B48C" 
                               FontSize="14" VerticalAlignment="Center" Margin="5"/>
                        </StackPanel>

                        <Button Grid.Column="2" Content="➕ ADD PRODUCT" Click="AddItem_Click" 
                        Background="#D2B48C" Foreground="#1E1E1E" FontWeight="Bold" 
                        Padding="15,0" Height="35" VerticalAlignment="Bottom" Margin="5">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="5"/>
                                </Style>
                            </Button.Resources>
                        </Button>
                    </Grid>
                </Border>

                <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="CANCEL" Click="Cancel_Click" Foreground="#888888" Background="Transparent" BorderThickness="0" Margin="0,0,15,0"/>
                    <Button Content="SAVE ORDER" Click="Save_Click" Width="140" Height="40" Background="#4CAF50" Foreground="White" FontWeight="Bold"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
