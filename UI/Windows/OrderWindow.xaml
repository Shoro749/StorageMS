<Window x:Class="UI.Windows.OrderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UI.Windows"
        mc:Ignorable="d"
        Title="OrderWindow" Height="500" Width="450"
        Background="#121212" WindowStartupLocation="CenterOwner" WindowStyle="None" AllowsTransparency="True">
    <Grid>
        <Grid.Resources>
            <Style x:Key="CommentTextBoxStyle" TargetType="TextBox">
                <Setter Property="Background" Value="#252525"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="BorderBrush" Value="#333333"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Padding" Value="10"/>
                <Setter Property="FontSize" Value="12"/>
                <Setter Property="CaretBrush" Value="#D2B48C"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="TextBox">
                            <Border x:Name="border" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="8">
                                <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="true">
                                    <Setter Property="BorderBrush" TargetName="border" Value="#D2B48C"/>
                                </Trigger>
                                <Trigger Property="IsFocused" Value="true">
                                    <Setter Property="BorderBrush" TargetName="border" Value="#D2B48C"/>
                                    <Setter Property="Background" Value="#2A2A2A"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style TargetType="DataGridColumnHeader">

                <Setter Property="Background" Value="#1E1E1E"/>

                <Setter Property="Foreground" Value="#D2B48C"/>

                <Setter Property="Padding" Value="10,5"/>

                <Setter Property="FontWeight" Value="Bold"/>

                <Setter Property="BorderThickness" Value="0,0,0,1"/>

                <Setter Property="BorderBrush" Value="#333333"/>

            </Style>



            <Style TargetType="ComboBox">
                <Setter Property="Foreground" Value="Black"/>
                <Setter Property="HorizontalContentAlignment" Value="Left"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
            </Style>

            <Style TargetType="ComboBoxItem">
                <Setter Property="Foreground" Value="Black"/>
                <Setter Property="Padding" Value="5"/>
            </Style>
        </Grid.Resources>

        <Border BorderBrush="#333333" BorderThickness="1" CornerRadius="15" Background="#1E1E1E">
            <Grid Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock x:Name="tb_windowTitle" Grid.Row="0" Text="РЕДАГУВАННЯ СКЛАДУ ЗАЯВКИ" Foreground="#D2B48C" 
                       FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>

                <Border Grid.Row="2" Background="#181818" CornerRadius="8" Padding="5">
                    <DataGrid x:Name="dg_OrderItems" Background="#1E1E1E" 
                              Foreground="White" RowBackground="#1E1E1E" AlternatingRowBackground="#252525"
                              BorderThickness="0" GridLinesVisibility="None" AutoGenerateColumns="False"
                              CanUserAddRows="False" SelectionMode="Single">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Товар" Binding="{Binding Product.Name}" Width="*"/>
                            <DataGridTextColumn Header="Доступно" Binding="{Binding Product.Stock}" Width="80"/>
                            <DataGridTemplateColumn Header="К-сть" Width="80">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBox Text="{Binding Quantity, UpdateSourceTrigger=PropertyChanged}" 
                                             Background="#2D2D2D" Foreground="White" BorderThickness="0"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Width="50">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="🗑" Foreground="#E53935" Background="Transparent" 
                                            BorderThickness="0" Click="RemoveItem_Click"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>

                <Border Grid.Row="3" Background="#252525" CornerRadius="8" Margin="0,15" Padding="10">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Margin="5">
                            <TextBlock Text="Виберіть товар" Foreground="#888888" FontSize="10"/>
                            <ComboBox x:Name="cb_ProductPicker" DisplayMemberPath="Name" 
                                  SelectionChanged="cb_ProductPicker_SelectionChanged" Height="35"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Margin="5" Width="100">
                            <TextBlock x:Name="lbl_Limit" Text="К-сть (max: })" Foreground="#888888" FontSize="10"/>
                            <TextBox x:Name="txt_InputQuantity" 
                                 Style="{StaticResource CommentTextBoxStyle}" 
                                 Height="35" 
                                 Text="1"
                                 VerticalContentAlignment="Center"
                                 HorizontalContentAlignment="Center"/>
                        </StackPanel>
                        <Button Grid.Column="2" Content="➕ ADD PRODUCT" Click="AddItem_Click" 
                            Background="#D2B48C" Foreground="#1E1E1E" FontWeight="Bold" 
                            Padding="15,0" Height="35" VerticalAlignment="Bottom" Margin="5">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="5"/>
                                </Style>
                            </Button.Resources>
                        </Button>
                    </Grid>
                </Border>

                <StackPanel Grid.Row="4" Margin="5,0,5,15">
                    <TextBlock Text="Коментар до заявки" Foreground="#555555" FontSize="10" Margin="0,0,0,2"/>
                    <TextBox x:Name="tb_Comment" 
                         Style="{StaticResource CommentTextBoxStyle}"
                         Height="60" 
                         TextWrapping="Wrap" 
                         AcceptsReturn="True" 
                         VerticalScrollBarVisibility="Auto"/>
                </StackPanel>

                <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="CANCEL" Click="Cancel_Click" Foreground="#888888" Background="Transparent" BorderThickness="0" Margin="0,0,15,0"/>
                    <Button Content="SAVE ORDER" Click="Save_Click" Width="140" Height="40" Background="#D2B48C" Foreground="#1E1E1E" FontWeight="Bold">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="8"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
